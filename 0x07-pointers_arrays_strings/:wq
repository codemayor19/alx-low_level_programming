#include "main.h"

/**
 * _strchr - return first occurence of character
 * @s: string of character
 * @c: character
 *
 * Return: character
 */
char *_strchr(char *s, char c)
{
	int i;

	i = 0;
	while (s[i] != '\0')
	{
		if (s[i] == c)
		{
			return (s + i);
		}
	}
	return ('\0');
}
